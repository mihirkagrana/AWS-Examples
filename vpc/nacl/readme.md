Todo: Hands on pending.
Create a subnet
Use DFT to launch EC2 instance using that subnet. EC2 should have public IP, IAM role, Apache installed.
Access EC2 IP in browser and make sure page is accessible.
Now, deny your IP from subnet and try to access IP, it should not be accessible.
